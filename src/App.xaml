<Window
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Apple 开发者账号管理工具"
  Width="1440"
  Height="900"
  MinWidth="1180"
  MinHeight="760"
  WindowStartupLocation="CenterScreen"
  Background="#F4F7FB">
  <Window.Resources>
    <Style TargetType="Button">
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Padding" Value="22,11"/>
      <Setter Property="MinWidth" Value="96"/>
      <Setter Property="MinHeight" Value="46"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18">
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.62"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="NavButtonStyle" TargetType="Button">
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Height" Value="52"/>
      <Setter Property="Padding" Value="24,0"/>
      <Setter Property="MinWidth" Value="0"/>
      <Setter Property="MinHeight" Value="0"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="18" SnapsToDevicePixels="True">
              <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.88"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.62"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Height" Value="42"/>
      <Setter Property="Padding" Value="14,9"/>
      <Setter Property="BorderBrush" Value="#DDE4EC"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ComboBoxItem">
      <Setter Property="Padding" Value="14,9"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Foreground" Value="#344255"/>
    </Style>
    <Style x:Key="SwitchToggleStyle" TargetType="ToggleButton">
      <Setter Property="Width" Value="56"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Border x:Name="Track" Width="56" Height="32" Background="#DDE6EF" CornerRadius="16">
              <Ellipse x:Name="Thumb" Width="24" Height="24" Fill="#FFFFFF" Margin="4" HorizontalAlignment="Left">
                <Ellipse.Effect>
                  <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.18"/>
                </Ellipse.Effect>
              </Ellipse>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Track" Property="Background" Value="#0A84FF"/>
                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Track" Property="Opacity" Value="0.90"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Height" Value="44"/>
      <Setter Property="Padding" Value="14,0"/>
      <Setter Property="BorderBrush" Value="#E2EAF2"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="Foreground" Value="#344255"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14">
                <Grid>
                  <TextBlock
                    Margin="{TemplateBinding Padding}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=SelectionBoxItem}"
                    Foreground="{TemplateBinding Foreground}"
                    FontSize="{TemplateBinding FontSize}"/>
                  <Path
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,14,0"
                    Fill="#8A97A8"
                    Data="M 0 0 L 8 0 L 4 5 Z"/>
                </Grid>
              </Border>
              <ToggleButton
                x:Name="ToggleButton"
                Focusable="False"
                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                ClickMode="Press"
                Background="Transparent"
                BorderThickness="0"
                FocusVisualStyle="{x:Null}">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent"/>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>
              <Popup
                x:Name="Popup"
                Placement="Bottom"
                IsOpen="{TemplateBinding IsDropDownOpen}"
                AllowsTransparency="True"
                Focusable="False"
                PopupAnimation="Fade">
                <Border
                  MinWidth="{TemplateBinding ActualWidth}"
                  MaxHeight="{TemplateBinding MaxDropDownHeight}"
                  Background="#FFFFFF"
                  BorderBrush="#E2EAF2"
                  BorderThickness="1"
                  CornerRadius="14"
                  Padding="4">
                  <ScrollViewer CanContentScroll="True">
                    <ItemsPresenter/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="DataGrid">
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="GridLinesVisibility" Value="Horizontal"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="RowHeight" Value="50"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="BorderBrush" Value="#E1E7EF"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="HorizontalGridLinesBrush" Value="#EEF2F6"/>
      <Setter Property="RowBackground" Value="#FFFFFF"/>
      <Setter Property="AlternatingRowBackground" Value="#FBFCFE"/>
      <Setter Property="SelectionUnit" Value="FullRow"/>
    </Style>
    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Height" Value="38"/>
      <Setter Property="Background" Value="#F8FAFC"/>
      <Setter Property="Foreground" Value="#607082"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Padding" Value="12,0"/>
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="10,0"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>
  </Window.Resources>

  <Grid Background="#F4F7FB">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="292"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="#FAFCFE" CornerRadius="0" Padding="24" BorderBrush="#E7EEF5" BorderThickness="0,0,1,0">
        <DockPanel LastChildFill="True">
          <StackPanel DockPanel.Dock="Top">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="56"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <Border Width="48" Height="48" CornerRadius="15" Background="#FFFFFF" BorderBrush="#DDEBFA" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Image x:Name="BrandLogo" Stretch="UniformToFill" SnapsToDevicePixels="True"/>
              </Border>
              <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="ASC Radar" Foreground="#111827" FontWeight="Black" FontSize="27"/>
                <TextBlock Text="多账号审核状态控制台" Foreground="#7A8796" FontSize="14" Margin="0,6,0,0"/>
              </StackPanel>
            </Grid>
            <TextBlock Text="通过备注快速识别账号，按账号查看 App 状态。" TextWrapping="Wrap" Foreground="#9AA6B5" FontSize="12" Margin="0,28,0,34"/>

            <Button x:Name="NavDashboard" Style="{StaticResource NavButtonStyle}" Background="#E8F3FF" Foreground="#152234" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <Path Width="18" Height="18" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M4,4 L10,4 L10,10 L4,10 Z M14,4 L20,4 L20,8 L14,8 Z M14,12 L20,12 L20,20 L14,20 Z M4,14 L10,14 L10,20 L4,20 Z"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="总览" VerticalAlignment="Center" Margin="14,0,0,0"/>
              </Grid>
            </Button>
            <Button x:Name="NavAccounts" Style="{StaticResource NavButtonStyle}" Background="Transparent" Foreground="#6A7889" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <Path Width="18" Height="18" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M8,11 L8,9 C8,6.8 9.8,5 12,5 C14.2,5 16,6.8 16,9 L16,11 M5,20 C5,16.7 8,14.5 12,14.5 C16,14.5 19,16.7 19,20"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="账号" VerticalAlignment="Center" Margin="14,0,0,0"/>
              </Grid>
            </Button>
            <Button x:Name="NavApps" Style="{StaticResource NavButtonStyle}" Background="Transparent" Foreground="#6A7889" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <Path Width="18" Height="18" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M6,5 L18,5 L18,19 L6,19 Z M6,10 L18,10 M9,14 L15,14"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="App 状态" VerticalAlignment="Center" Margin="14,0,0,0"/>
              </Grid>
            </Button>
            <Button x:Name="NavAlerts" Style="{StaticResource NavButtonStyle}" Background="Transparent" Foreground="#6A7889" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <Path Width="18" Height="18" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M12,5 L12,14 M12,18 L12,18.1 M5,20 L19,20 L12,4 Z"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="提醒" VerticalAlignment="Center" Margin="14,0,0,0"/>
              </Grid>
            </Button>
            <Button x:Name="NavSettings" Style="{StaticResource NavButtonStyle}" Background="Transparent" Foreground="#6A7889" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                  <Path Width="18" Height="18" Stretch="Uniform" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.9" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Data="M12,15 C13.66,15 15,13.66 15,12 C15,10.34 13.66,9 12,9 C10.34,9 9,10.34 9,12 C9,13.66 10.34,15 12,15 Z M12,5 L12,7 M12,17 L12,19 M5,12 L7,12 M17,12 L19,12 M7.8,7.8 L9.2,9.2 M14.8,14.8 L16.2,16.2 M16.2,7.8 L14.8,9.2 M9.2,14.8 L7.8,16.2"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="设置" VerticalAlignment="Center" Margin="14,0,0,0"/>
              </Grid>
            </Button>
          </StackPanel>
        </DockPanel>
      </Border>

      <Border Grid.Column="1" Margin="0" Background="#F8FAFD" CornerRadius="0" Padding="26" BorderBrush="#E7EEF5" BorderThickness="0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="24" Padding="20" Margin="0,0,0,26" BorderBrush="#EDF2F7" BorderThickness="1">
            <DockPanel>
              <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Apple 开发者账号管理" FontSize="22" FontWeight="Black" Foreground="#101928"/>
                <TextBlock Text="集中同步 App Store Connect 的版本、构建与审核状态。" Foreground="#7A8796" FontSize="13" Margin="0,6,0,0"/>
              </StackPanel>
              <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <CheckBox x:Name="AutoSyncToggle" Content="自动同步" IsChecked="True" VerticalAlignment="Center" Foreground="#516173" FontWeight="SemiBold" Margin="0,0,12,0"/>
                <TextBlock x:Name="AutoSyncStatusText" Text="版本或产品页面优化：审核中 1 分钟，等待审核 5 分钟，其他 15 分钟" VerticalAlignment="Center" Foreground="#8A97A8" FontSize="12" Margin="0,0,16,0"/>
                <Button x:Name="SyncAllBtn" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" Margin="0,0,12,0">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="SyncAllSpinner" Text="◜" Visibility="Collapsed" FontSize="15" FontWeight="Black" Margin="0,0,8,0"/>
                    <TextBlock x:Name="SyncAllText" Text="同步全部"/>
                  </StackPanel>
                </Button>
                <Button x:Name="AddAccountBtn" Content="添加账号" Background="#0A84FF" Foreground="White"/>
              </StackPanel>
            </DockPanel>
          </Border>

          <Grid Grid.Row="1">
            <Grid x:Name="DashboardView">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,18">
                <Border Background="#FFFFFF" CornerRadius="22" Padding="22" Margin="0,0,16,0">
                  <StackPanel>
                    <TextBlock Text="账号" Foreground="#7A8796" FontSize="13"/>
                    <TextBlock x:Name="MetricAccounts" Text="0" FontSize="38" FontWeight="Black" Foreground="#101928" Margin="0,10,0,3"/>
                    <TextBlock x:Name="MetricConnected" Text="0 个已连接" Foreground="#8A97A8" FontSize="12"/>
                  </StackPanel>
                </Border>
                <Border Background="#FFFFFF" CornerRadius="22" Padding="22" Margin="0,0,16,0">
                  <StackPanel>
                    <TextBlock Text="App" Foreground="#7A8796" FontSize="13"/>
                    <TextBlock x:Name="MetricApps" Text="0" FontSize="38" FontWeight="Black" Foreground="#101928" Margin="0,10,0,3"/>
                    <TextBlock Text="已纳入监控" Foreground="#8A97A8" FontSize="12"/>
                  </StackPanel>
                </Border>
                <Border Background="#FFFFFF" CornerRadius="22" Padding="22" Margin="0,0,16,0">
                  <StackPanel>
                    <TextBlock Text="版本审核中" Foreground="#7A8796" FontSize="13"/>
                    <TextBlock x:Name="MetricReview" Text="0" FontSize="38" FontWeight="Black" Foreground="#B45F00" Margin="0,10,0,3"/>
                    <TextBlock Text="App 版本" Foreground="#8A97A8" FontSize="12"/>
                  </StackPanel>
                </Border>
                <Border Background="#FFFFFF" CornerRadius="22" Padding="22" Margin="0,0,16,0">
                  <StackPanel>
                    <TextBlock Text="产品页面优化中" Foreground="#7A8796" FontSize="13"/>
                    <TextBlock x:Name="MetricExtraReview" Text="0" FontSize="38" FontWeight="Black" Foreground="#2455D6" Margin="0,10,0,3"/>
                    <TextBlock Text="产品页等审核" Foreground="#8A97A8" FontSize="12"/>
                  </StackPanel>
                </Border>
                <Border Background="#FFFFFF" CornerRadius="22" Padding="22">
                  <StackPanel>
                    <TextBlock Text="待处理" Foreground="#7A8796" FontSize="13"/>
                    <TextBlock x:Name="MetricAlerts" Text="0" FontSize="38" FontWeight="Black" Foreground="#BD2838" Margin="0,10,0,3"/>
                    <TextBlock x:Name="MetricRejected" Text="0 个被拒" Foreground="#8A97A8" FontSize="12"/>
                  </StackPanel>
                </Border>
              </UniformGrid>

              <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="1.35*"/>
                  <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="24" Padding="22" Margin="0,0,18,0">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,14">
                      <TextBlock Text="需要处理" FontSize="20" FontWeight="Black" Foreground="#101928"/>
                      <TextBlock Text="审核异常、同步失败和状态停留过久会出现在这里。" Foreground="#8A97A8" FontSize="13" Margin="0,5,0,0"/>
                    </StackPanel>
                    <ListBox x:Name="DashboardAlertsList" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto">
                      <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                          <Setter Property="Padding" Value="0"/>
                          <Setter Property="Margin" Value="0,0,0,12"/>
                          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                      </ListBox.ItemContainerStyle>
                      <ListBox.ItemTemplate>
                        <DataTemplate>
                          <Border Background="#F8FBFE" CornerRadius="16" Padding="16" BorderBrush="#E6EDF5" BorderThickness="1">
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                              </Grid.ColumnDefinitions>
                              <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Text="{Binding title}" FontSize="15" FontWeight="Bold" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding message}" Foreground="#7A8796" FontSize="13" Margin="0,6,0,0" TextWrapping="Wrap"/>
                              </StackPanel>
                              <Button Grid.Column="1" Tag="{Binding id}" Content="已处理" Width="72" Height="32" MinWidth="0" MinHeight="0" Padding="8,4" FontSize="12" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" VerticalAlignment="Center"/>
                            </Grid>
                          </Border>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                  </Grid>
                </Border>
                <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="24" Padding="22">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,0,0,14">
                      <TextBlock Text="最近 App" FontSize="20" FontWeight="Black" Foreground="#101928"/>
                      <TextBlock Text="按更新时间排序。" Foreground="#8A97A8" FontSize="13" Margin="0,5,0,0"/>
                    </StackPanel>
                    <ListBox x:Name="RecentAppsGrid" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto">
                      <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                          <Setter Property="Padding" Value="0"/>
                          <Setter Property="Margin" Value="0,0,0,12"/>
                          <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                      </ListBox.ItemContainerStyle>
                      <ListBox.ItemTemplate>
                        <DataTemplate>
                          <Border Background="#F8FBFE" CornerRadius="16" Padding="14" BorderBrush="#E6EDF5" BorderThickness="1">
                            <Grid>
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                              </Grid.ColumnDefinitions>
                              <Border Grid.Column="0" Width="38" Height="38" CornerRadius="12" Background="#E8F3FF" Margin="0,0,12,0">
                                <Grid>
                                  <Image Source="{Binding iconPath}" Visibility="{Binding appIconVisibility}" Stretch="UniformToFill"/>
                                  <TextBlock Text="{Binding appInitial}" Visibility="{Binding appInitialVisibility}" Foreground="#0A84FF" FontSize="18" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                              </Border>
                              <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                <TextBlock Text="{Binding name}" FontSize="15" FontWeight="Bold" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding versionDisplay}" Foreground="#8A97A8" FontSize="12" Margin="0,5,0,0"/>
                              </StackPanel>
                              <Border Grid.Column="2" Background="{Binding statusBackground}" CornerRadius="11" Padding="10,6" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding statusLabel}" Foreground="{Binding statusBrush}" FontWeight="Bold" FontSize="12"/>
                              </Border>
                            </Grid>
                          </Border>
                        </DataTemplate>
                      </ListBox.ItemTemplate>
                    </ListBox>
                  </Grid>
                </Border>
              </Grid>
            </Grid>

            <Grid x:Name="AccountsView" Visibility="Collapsed">
              <Border Background="#FFFFFF" CornerRadius="24" Padding="24">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                  </Grid.RowDefinitions>
                  <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                      <TextBlock Text="账号连接" FontSize="24" FontWeight="Black" Foreground="#101928"/>
                      <TextBlock Text="备注优先展示，方便在大量账号中快速区分。" Foreground="#8A97A8" FontSize="13" Margin="0,6,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                      <Button x:Name="SyncSelectedAccountBtn" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                          <TextBlock x:Name="SyncSelectedSpinner" Text="◜" Visibility="Collapsed" FontSize="15" FontWeight="Black" Margin="0,0,8,0"/>
                          <TextBlock x:Name="SyncSelectedText" Text="同步选中账号"/>
                        </StackPanel>
                      </Button>
                      <Button x:Name="EditSelectedAccountBtn" Content="编辑账号" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" Margin="0,0,12,0"/>
                      <Button x:Name="AddAccountBtn2" Content="添加账号" Background="#0A84FF" Foreground="White"/>
                    </StackPanel>
                  </Grid>
                  <ListBox x:Name="AccountsGrid" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                      <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,14"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                      </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                      <DataTemplate>
                        <Border Background="#FFFFFF" CornerRadius="20" Padding="18" BorderBrush="#E6EDF5" BorderThickness="1">
                          <Grid>
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="2.1*"/>
                              <ColumnDefinition Width="1.3*"/>
                              <ColumnDefinition Width="1.1*"/>
                              <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                              <TextBlock Text="{Binding noteDisplay}" FontSize="20" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                              <TextBlock Text="{Binding name}" Foreground="#7A8796" FontSize="13" Margin="0,7,0,0" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                              <TextBlock Text="Issuer ID" Foreground="#A0ACBA" FontSize="12"/>
                              <TextBlock Text="{Binding issuerId}" Foreground="#344255" FontSize="13" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="20,0,0,0">
                              <TextBlock Text="Key ID / 同步" Foreground="#A0ACBA" FontSize="12"/>
                              <TextBlock Text="{Binding keyId}" Foreground="#344255" FontWeight="Bold" FontSize="13" Margin="0,6,0,0"/>
                              <TextBlock Text="{Binding lastSyncText}" Foreground="#8A97A8" FontSize="12" Margin="0,6,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="3" Background="{Binding accountStatusBackground}" CornerRadius="12" Padding="12,7" VerticalAlignment="Center" Margin="20,0,0,0">
                              <TextBlock Text="{Binding statusText}" Foreground="{Binding accountStatusBrush}" FontWeight="Bold"/>
                            </Border>
                          </Grid>
                        </Border>
                      </DataTemplate>
                    </ListBox.ItemTemplate>
                  </ListBox>
                  <Button x:Name="DeleteSelectedAccountBtn" Grid.Row="2" Content="删除选中账号" Width="112" Height="32" MinWidth="0" MinHeight="0" Padding="10,4" FontSize="12" Background="Transparent" Foreground="#C34B5A" BorderBrush="#F1CED4" BorderThickness="1" HorizontalAlignment="Right" Margin="0,10,0,0"/>
                </Grid>
              </Border>
            </Grid>

            <Grid x:Name="AppsView" Visibility="Collapsed">
              <Border Background="#FFFFFF" CornerRadius="24" Padding="24">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                  </Grid.RowDefinitions>
                  <Grid Margin="0,0,0,22">
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Margin="0,0,0,16">
                      <TextBlock Text="App 状态" FontSize="28" FontWeight="Black" Foreground="#101928"/>
                      <TextBlock x:Name="AppScopeText" Text="选择账号后查看对应 App。" Foreground="#8A97A8" FontSize="13" Margin="0,7,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                    <Border Grid.Row="1" HorizontalAlignment="Left" Background="#FFFFFF" CornerRadius="20" Padding="16" BorderBrush="#E6EDF5" BorderThickness="1">
                      <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                        <StackPanel Margin="0,0,12,0">
                          <TextBlock Text="账号" Foreground="#7A8796" FontSize="12" Margin="0,0,0,5"/>
                          <ComboBox x:Name="AccountFilter" Width="420"/>
                        </StackPanel>
                        <StackPanel>
                          <TextBlock Text="状态" Foreground="#7A8796" FontSize="12" Margin="0,0,0,5"/>
                          <ComboBox x:Name="StatusFilter" Width="170"/>
                        </StackPanel>
                      </StackPanel>
                    </Border>
                  </Grid>
                  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="AppsGrid">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <StackPanel/>
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Border Margin="0,0,0,14" Background="#FFFFFF" CornerRadius="20" Padding="18" BorderBrush="#E6EDF5" BorderThickness="1" Cursor="Hand">
                            <StackPanel>
                              <Grid>
                                <Grid.ColumnDefinitions>
                                  <ColumnDefinition Width="2.0*"/>
                                  <ColumnDefinition Width="1.6*"/>
                                  <ColumnDefinition Width="1.0*"/>
                                  <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                  <StackPanel Orientation="Horizontal">
                                    <Border Width="46" Height="46" CornerRadius="14" Background="#E8F3FF" Margin="0,0,14,0">
                                      <Grid>
                                        <Image Source="{Binding iconPath}" Visibility="{Binding appIconVisibility}" Stretch="UniformToFill"/>
                                        <TextBlock Text="{Binding appInitial}" Visibility="{Binding appInitialVisibility}" Foreground="#0A84FF" FontSize="22" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                      </Grid>
                                    </Border>
                                    <StackPanel>
                                      <TextBlock Text="{Binding name}" FontSize="20" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                                      <TextBlock Text="{Binding bundleId}" Foreground="#7A8796" FontSize="13" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                  </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="22,0,0,0">
                                  <TextBlock Text="账号" Foreground="#A0ACBA" FontSize="12"/>
                                  <TextBlock Text="{Binding accountDisplay}" Foreground="#344255" FontSize="13" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <Grid Grid.Column="2" Margin="22,0,0,0">
                                  <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                  </Grid.ColumnDefinitions>
                                  <StackPanel Grid.Column="0">
                                    <TextBlock Text="版本" Foreground="#A0ACBA" FontSize="12"/>
                                    <TextBlock Text="{Binding versionDisplay}" Foreground="#344255" FontWeight="Bold" FontSize="15" Margin="0,5,0,0"/>
                                  </StackPanel>
                                  <StackPanel Grid.Column="1">
                                    <TextBlock Text="Build" Foreground="#A0ACBA" FontSize="12"/>
                                    <TextBlock Text="{Binding buildDisplay}" Foreground="#344255" FontWeight="Bold" FontSize="15" Margin="0,5,0,0"/>
                                  </StackPanel>
                                </Grid>
                                <StackPanel Grid.Column="3" Margin="24,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right">
                                  <Border Background="{Binding statusBackground}" CornerRadius="12" Padding="12,7" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding statusLabel}" Foreground="{Binding statusBrush}" FontWeight="Bold"/>
                                  </Border>
                                  <TextBlock Text="{Binding updatedText}" Foreground="#A0ACBA" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Right"/>
                                </StackPanel>
                              </Grid>
                              <Border Visibility="{Binding extraReviewsVisibility}" Background="#F8FBFE" BorderBrush="#E6EDF5" BorderThickness="1" CornerRadius="16" Padding="12" Margin="60,16,0,0">
                                <Grid>
                                  <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                  </Grid.ColumnDefinitions>
                                  <TextBlock Grid.Column="0" Text="产品页面优化" Foreground="#7A8796" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,14,0"/>
                                  <TextBlock Grid.Column="1" Text="{Binding extraReviewDisplay}" Foreground="#344255" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                  <Border Grid.Column="2" Background="{Binding extraReviewStatusBackground}" CornerRadius="12" Padding="10,6" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding extraReviewStatusLabel}" Foreground="{Binding extraReviewStatusBrush}" FontSize="12" FontWeight="Black"/>
                                  </Border>
                                </Grid>
                              </Border>
                            </StackPanel>
                          </Border>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </ScrollViewer>
                </Grid>
              </Border>
            </Grid>

            <Grid x:Name="AppDetailView" Visibility="Collapsed">
              <Border Background="#FFFFFF" CornerRadius="24" Padding="24">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                  </Grid.RowDefinitions>

                  <Grid Grid.Row="0" Margin="0,0,0,22">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto"/>
                      <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BackToAppsBtn" Content="返回" Width="74" Height="36" MinWidth="0" MinHeight="0" Padding="10,4" FontSize="13" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" Margin="0,0,16,0"/>
                    <StackPanel Grid.Column="1">
                      <TextBlock Text="App 详情" FontSize="28" FontWeight="Black" Foreground="#101928"/>
                      <TextBlock Text="版本和产品页面优化的状态变化时间。" Foreground="#8A97A8" FontSize="13" Margin="0,7,0,0"/>
                    </StackPanel>
                  </Grid>

                  <Border Grid.Row="1" Background="#F8FBFE" CornerRadius="20" Padding="18" BorderBrush="#E6EDF5" BorderThickness="1" Margin="0,0,0,18">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.1*"/>
                        <ColumnDefinition Width="0.8*"/>
                        <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <Border Grid.Column="0" Width="58" Height="58" CornerRadius="16" Background="#E8F3FF" Margin="0,0,16,0">
                        <Grid>
                          <Image x:Name="DetailAppIcon" Stretch="UniformToFill"/>
                          <TextBlock x:Name="DetailAppInitial" Foreground="#0A84FF" FontSize="25" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                      </Border>
                      <StackPanel Grid.Column="1">
                        <TextBlock x:Name="DetailAppName" Text="-" FontSize="24" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="DetailBundleId" Text="-" Foreground="#7A8796" FontSize="13" Margin="0,7,0,0" TextTrimming="CharacterEllipsis"/>
                      </StackPanel>
                      <StackPanel Grid.Column="2" Margin="24,0,0,0">
                        <TextBlock Text="账号" Foreground="#A0ACBA" FontSize="12"/>
                        <TextBlock x:Name="DetailAccountName" Text="-" Foreground="#344255" FontSize="13" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                      </StackPanel>
                      <Grid Grid.Column="3" Margin="24,0,0,0">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                          <TextBlock Text="版本" Foreground="#A0ACBA" FontSize="12"/>
                          <TextBlock x:Name="DetailVersion" Text="-" Foreground="#344255" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                          <TextBlock Text="Build" Foreground="#A0ACBA" FontSize="12"/>
                          <TextBlock x:Name="DetailBuild" Text="-" Foreground="#344255" FontWeight="Bold" FontSize="16" Margin="0,5,0,0"/>
                        </StackPanel>
                      </Grid>
                      <StackPanel Grid.Column="4" Margin="24,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Border x:Name="DetailMainStatusPill" CornerRadius="12" Padding="12,7" HorizontalAlignment="Right">
                          <TextBlock x:Name="DetailMainStatusText" FontWeight="Bold"/>
                        </Border>
                        <TextBlock x:Name="DetailMainChangedAt" Foreground="#A0ACBA" FontSize="12" Margin="0,8,0,0" HorizontalAlignment="Right"/>
                      </StackPanel>
                    </Grid>
                  </Border>

                  <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                      </Grid.ColumnDefinitions>

                      <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="20" Padding="20" BorderBrush="#E6EDF5" BorderThickness="1" Margin="0,0,14,0">
                        <Grid>
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                          </Grid.RowDefinitions>
                          <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="版本时间线" FontSize="20" FontWeight="Black" Foreground="#101928"/>
                            <TextBlock Text="按时间记录每次版本状态变化。" Foreground="#8A97A8" FontSize="12" Margin="0,6,0,0"/>
                          </StackPanel>
                          <ListBox x:Name="MainStatusHistoryList" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                              <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                              </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                              <DataTemplate>
                                <Border Background="#F8FBFE" CornerRadius="16" Padding="14" BorderBrush="#E6EDF5" BorderThickness="1">
                                  <Grid>
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="*"/>
                                      <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                      <TextBlock Text="{Binding title}" FontSize="15" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                                      <TextBlock Text="{Binding timeText}" Foreground="#7A8796" FontSize="12" Margin="0,6,0,0"/>
                                      <TextBlock Text="{Binding changeText}" Foreground="#8A97A8" FontSize="12" Margin="0,5,0,0"/>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="{Binding statusBackground}" CornerRadius="12" Padding="10,6" VerticalAlignment="Center">
                                      <TextBlock Text="{Binding statusLabel}" Foreground="{Binding statusBrush}" FontWeight="Black" FontSize="12"/>
                                    </Border>
                                  </Grid>
                                </Border>
                              </DataTemplate>
                            </ListBox.ItemTemplate>
                          </ListBox>
                        </Grid>
                      </Border>

                      <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="20" Padding="20" BorderBrush="#E6EDF5" BorderThickness="1" Margin="14,0,0,0">
                        <Grid>
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                          </Grid.RowDefinitions>
                          <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="产品页面优化时间线" FontSize="20" FontWeight="Black" Foreground="#101928"/>
                            <TextBlock Text="只显示真实存在的产品页面优化、自定义产品页等审核项。" Foreground="#8A97A8" FontSize="12" Margin="0,6,0,0"/>
                          </StackPanel>
                          <ListBox x:Name="ExtraStatusHistoryList" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                              <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,12"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                              </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                              <DataTemplate>
                                <Border Background="#F8FBFE" CornerRadius="16" Padding="14" BorderBrush="#E6EDF5" BorderThickness="1">
                                  <Grid>
                                    <Grid.ColumnDefinitions>
                                      <ColumnDefinition Width="*"/>
                                      <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                      <TextBlock Text="{Binding title}" FontSize="15" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                                      <TextBlock Text="{Binding timeText}" Foreground="#7A8796" FontSize="12" Margin="0,6,0,0"/>
                                      <TextBlock Text="{Binding changeText}" Foreground="#8A97A8" FontSize="12" Margin="0,5,0,0"/>
                                    </StackPanel>
                                    <Border Grid.Column="1" Background="{Binding statusBackground}" CornerRadius="12" Padding="10,6" VerticalAlignment="Center">
                                      <TextBlock Text="{Binding statusLabel}" Foreground="{Binding statusBrush}" FontWeight="Black" FontSize="12"/>
                                    </Border>
                                  </Grid>
                                </Border>
                              </DataTemplate>
                            </ListBox.ItemTemplate>
                          </ListBox>
                        </Grid>
                      </Border>
                    </Grid>
                  </ScrollViewer>
                </Grid>
              </Border>
            </Grid>

            <Grid x:Name="AlertsView" Visibility="Collapsed">
              <Border Background="#FFFFFF" CornerRadius="24" Padding="24">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                  </Grid.RowDefinitions>
                  <StackPanel Margin="0,0,0,18">
                    <TextBlock Text="提醒中心" FontSize="24" FontWeight="Black" Foreground="#101928"/>
                    <TextBlock Text="处理被拒、同步失败和长时间卡住的状态。" Foreground="#8A97A8" FontSize="13" Margin="0,6,0,0"/>
                  </StackPanel>
                  <ListBox x:Name="AlertsGrid" Grid.Row="1" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                      <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,14"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                      </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                      <DataTemplate>
                        <Border Background="#FFFFFF" CornerRadius="20" Padding="18" BorderBrush="#E6EDF5" BorderThickness="1">
                          <Grid>
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="1.4*"/>
                              <ColumnDefinition Width="1.2*"/>
                              <ColumnDefinition Width="2*"/>
                              <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                              <TextBlock Text="{Binding title}" FontSize="18" FontWeight="Black" Foreground="#101928" TextTrimming="CharacterEllipsis"/>
                              <TextBlock Text="{Binding appName}" Foreground="#7A8796" FontSize="13" Margin="0,7,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                              <TextBlock Text="账号" Foreground="#A0ACBA" FontSize="12"/>
                              <TextBlock Text="{Binding accountName}" Foreground="#344255" FontSize="13" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="20,0,0,0">
                              <TextBlock Text="内容" Foreground="#A0ACBA" FontSize="12"/>
                              <TextBlock Text="{Binding message}" Foreground="#344255" FontSize="13" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Margin="20,0,0,0" VerticalAlignment="Center">
                              <Border Background="#FFF0F2" CornerRadius="12" Padding="12,7" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding severity}" Foreground="#BD2838" FontWeight="Bold"/>
                              </Border>
                              <Button Tag="{Binding id}" Content="已处理" Width="72" Height="32" MinWidth="0" MinHeight="0" Padding="8,4" FontSize="12" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" HorizontalAlignment="Right" Margin="0,10,0,0"/>
                            </StackPanel>
                          </Grid>
                        </Border>
                      </DataTemplate>
                    </ListBox.ItemTemplate>
                  </ListBox>
                </Grid>
              </Border>
            </Grid>

            <Grid x:Name="SettingsView" Visibility="Collapsed">
              <Border Background="#FFFFFF" CornerRadius="24" Padding="24">
                <Grid>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                  </Grid.RowDefinitions>
                  <StackPanel Margin="0,0,0,22">
                    <TextBlock Text="设置" FontSize="24" FontWeight="Black" Foreground="#101928"/>
                    <TextBlock Text="调整窗口关闭行为和后台同步方式。" Foreground="#8A97A8" FontSize="13" Margin="0,6,0,0"/>
                  </StackPanel>
                  <StackPanel Grid.Row="1">
                    <Border Background="#F8FBFE" CornerRadius="20" Padding="20" BorderBrush="#E6EDF5" BorderThickness="1" Margin="0,0,0,14">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                          <TextBlock Text="关闭按钮缩到托盘" FontSize="18" FontWeight="Black" Foreground="#101928"/>
                          <TextBlock Text="开启后点击右上角关闭按钮不会退出程序，自动同步会继续在后台运行。" Foreground="#7A8796" FontSize="13" TextWrapping="Wrap" Margin="0,7,0,0"/>
                          <TextBlock x:Name="SettingsTrayStatusText" Text="当前：关闭窗口后缩到右下角托盘" Foreground="#0A84FF" FontSize="12" FontWeight="Bold" Margin="0,12,0,0"/>
                        </StackPanel>
                        <ToggleButton x:Name="CloseToTrayToggle" Grid.Column="1" Style="{StaticResource SwitchToggleStyle}" IsChecked="True" VerticalAlignment="Center"/>
                      </Grid>
                    </Border>
                    <Border Background="#F8FBFE" CornerRadius="20" Padding="20" BorderBrush="#E6EDF5" BorderThickness="1" Margin="0,0,0,14">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                          <TextBlock Text="自动发现新 App" FontSize="18" FontWeight="Black" Foreground="#101928"/>
                          <TextBlock Text="开启后会低频扫描账号下是否出现全新 App。发现后会加入列表，之后按状态规则自动同步。" Foreground="#7A8796" FontSize="13" TextWrapping="Wrap" Margin="0,7,0,0"/>
                          <TextBlock x:Name="SettingsDiscoverStatusText" Text="当前：每 6 小时低频扫描一次新 App" Foreground="#0A84FF" FontSize="12" FontWeight="Bold" Margin="0,12,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                          <ComboBox x:Name="AppDiscoveryIntervalCombo" Width="104" Height="36" Margin="0,0,14,0">
                            <ComboBoxItem Content="3 小时" Tag="3"/>
                            <ComboBoxItem Content="6 小时" Tag="6"/>
                            <ComboBoxItem Content="9 小时" Tag="9"/>
                          </ComboBox>
                          <Button x:Name="DiscoverAppsNowBtn" Content="立即发现" Width="92" Height="36" MinWidth="0" MinHeight="0" Padding="12,6" FontSize="13" Background="#FFFFFF" Foreground="#516173" BorderBrush="#E1E7EF" BorderThickness="1" Margin="0,0,14,0"/>
                          <ToggleButton x:Name="AutoDiscoverAppsToggle" Style="{StaticResource SwitchToggleStyle}" IsChecked="False"/>
                        </StackPanel>
                      </Grid>
                    </Border>
                    <Border Background="#FFFFFF" CornerRadius="20" Padding="20" BorderBrush="#E6EDF5" BorderThickness="1">
                      <StackPanel>
                        <TextBlock Text="托盘操作" FontSize="18" FontWeight="Black" Foreground="#101928"/>
                        <TextBlock Text="缩到托盘后，可以双击右下角图标打开主界面，也可以在托盘菜单里选择退出程序。" Foreground="#7A8796" FontSize="13" TextWrapping="Wrap" Margin="0,7,0,0"/>
                      </StackPanel>
                    </Border>
                  </StackPanel>
                </Grid>
              </Border>
            </Grid>
          </Grid>
        </Grid>
      </Border>
  </Grid>
</Window>






























